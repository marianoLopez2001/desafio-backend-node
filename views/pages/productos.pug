html 
    head 
        include ../partials/head
    body 
        header 
            include ../partials/header
    main  
        h2(class="text-center m-4") Productos
        if arrayProductos.length === 0
            h4(class="text-center m-4") No hay productos
        each i in arrayProductos
            ul(style="list-style: none; background-color: #F8F9FA; border-radius: 0.5rem; padding: 0.2rem; margin: 1rem;")
                li(class="d-flex align-items-center", style="font-size: 20px; margin: 0.5rem") 
                    p="Titulo: " + i.title + ", Precio: " + i.price + ", Id:" + i.id
                    img(src=i.thumbnail, width=50)
    footer 
        include ../partials/footer


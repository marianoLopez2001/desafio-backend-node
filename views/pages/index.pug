html 
    head 
        include ../partials/head
    body 
        header 
            include ../partials/header
    main 
        h2(class="text-center m-5")="Formulario"
        form(class="d-flex flex-column text-center w-50 m-auto bg-light p-3 rounded" action="/productos" method="POST")
            input(placeholder="Title" class="rounded p-1 m-1" type="text" name="title")
            input(placeholder="Price" class="rounded p-1 m-1" type="text" name="price")
            input(placeholder="Thumbnail" class="rounded p-1 m-1" type="text" name="thumbnail")
            button(class="btn btn-primary" type="submit") Enviar
    footer 
        include ../partials/footer

